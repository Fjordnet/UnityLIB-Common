using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Fjord.Common.Enums
{
    /// <summary>
    /// Helper for enumerating directions with four values (two dimensions).
    /// </summary>
    public enum Direction4
    {
        Up = 0,
        Down = 1,
        Left = 2,
        Right =3,
    }
}